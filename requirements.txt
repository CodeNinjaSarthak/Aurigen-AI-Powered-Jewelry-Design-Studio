# -------------------------------------------------------
# Aurigen — Python 3.10 / CUDA 11.8
# Install PyTorch first (before pip install -r):
#   pip install torch==2.2.2 torchvision==0.17.2 \
#       --index-url https://download.pytorch.org/whl/cu118
# -------------------------------------------------------

# Diffusion stack
diffusers==0.27.2
transformers==4.40.2
accelerate==0.29.3
safetensors==0.4.3

# Vision / data
Pillow==10.3.0
opencv-python==4.9.0.80
numpy==1.26.4

# Dataset utilities
datasets==2.19.1

# App
streamlit==1.33.0

# Memory-efficient attention (CUDA only, skip on macOS/CPU)
xformers==0.0.25; sys_platform != "darwin"
